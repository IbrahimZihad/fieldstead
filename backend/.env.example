PORT=5000
JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRES_IN=7d
# SQLite file path (used by default, zero-config)
DB_STORAGE=./data/database.sqlite
# CORS: the URL your Next.js frontend runs on
CLIENT_URL=http://localhost:3000
